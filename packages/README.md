# Packages

- `@powerchain/database` — Prisma/PostgreSQL client boundary.
- `@powerchain/runtime` — shared runtime/worker utilities.

Application services live under `apps/`; reusable infrastructure belongs here. All first-party packages remain version `1.0.0`.
