# Generated build artifacts

This directory is reserved for local/generated build output. Canonical source
lives under `apps/`, `packages/`, `programs/`, and `contracts/`.

Do not commit deployment keypairs, private keys, or generated mainnet artifacts.
