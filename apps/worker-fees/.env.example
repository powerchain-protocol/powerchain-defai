# PowerChain uses the canonical root environment contract.
# See ../../.env.example and configure deployment secrets in the runtime platform.

# Set true during maintenance/deploy drain windows. Workers keep heartbeats but claim no new jobs.
POWERCHAIN_WORKER_DRAIN_MODE=false
