"use client";
import {useCallback,useEffect,useRef,useState} from "react";
import {fetchLiquidityPositions,type LiquidityPositions} from "@/lib/data/liquidity";
export function useLiquidity(solanaAddress?:string|null){const[data,setData]=useState<LiquidityPositions|null>(null);const[loading,setLoading]=useState(false);const[error,setError]=useState<string|null>(null);const ref=useRef<AbortController|null>(null);const refresh=useCallback(async()=>{if(!solanaAddress){setData(null);setError(null);return}ref.current?.abort();const abort=new AbortController();ref.current=abort;setLoading(true);try{setData(await fetchLiquidityPositions(solanaAddress,abort.signal));setError(null)}catch(error){if(!abort.signal.aborted)setError(error instanceof Error?error.message:"LIQUIDITY_POSITIONS_UNAVAILABLE")}finally{if(!abort.signal.aborted)setLoading(false)}},[solanaAddress]);useEffect(()=>{void refresh();return()=>ref.current?.abort()},[refresh]);return{data,loading,error,refresh};}
