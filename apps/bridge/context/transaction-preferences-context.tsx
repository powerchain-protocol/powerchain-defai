"use client";import {createContext,useContext,useEffect,useMemo,useState,type ReactNode} from "react";import {DEFAULT_SWAP_CHAIN} from "@/constants/trading";type SwapChain="SOLANA"|"SUI";type Preferences={defaultSwapChain:SwapChain;setDefaultSwapChain:(chain:SwapChain)=>void;showAdvancedRouting:boolean;setShowAdvancedRouting:(value:boolean)=>void};const Context=createContext<Preferences|null>(null);const KEY="powerchain.transaction-preferences.v1";export function TransactionPreferencesProvider({children}:{children:ReactNode}){const[defaultSwapChain,setDefaultSwapChain]=useState<SwapChain>(DEFAULT_SWAP_CHAIN);const[showAdvancedRouting,setShowAdvancedRouting]=useState(false);useEffect(()=>{try{const raw=localStorage.getItem(KEY);if(raw){const value=JSON.parse(raw) as {defaultSwapChain?:unknown;showAdvancedRouting?:unknown};if(value.defaultSwapChain==="SOLANA"||value.defaultSwapChain==="SUI")setDefaultSwapChain(value.defaultSwapChain);if(typeof value.showAdvancedRouting==="boolean")setShowAdvancedRouting(value.showAdvancedRouting)}}catch{}},[]);useEffect(()=>{try{localStorage.setItem(KEY,JSON.stringify({defaultSwapChain,showAdvancedRouting}))}catch{}},[defaultSwapChain,showAdvancedRouting]);const value=useMemo(()=>({defaultSwapChain,setDefaultSwapChain,showAdvancedRouting,setShowAdvancedRouting}),[defaultSwapChain,showAdvancedRouting]);return <Context.Provider value={value}>{children}</Context.Provider>}export function useTransactionPreferences(){const value=useContext(Context);if(!value)throw new Error("TRANSACTION_PREFERENCES_PROVIDER_REQUIRED");return value}
