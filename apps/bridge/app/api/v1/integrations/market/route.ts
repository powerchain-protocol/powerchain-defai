import { birdeyeStatus,coinMarketCapStatus,dexScreenerStatus } from "@powerchain/backend";import {ok,requestId} from "@/server/http";export const dynamic="force-dynamic";export async function GET(req:Request){return ok({providers:{birdeye:birdeyeStatus(),coinmarketcap:coinMarketCapStatus(),dexscreener:dexScreenerStatus()},authoritativeForBridgeAccounting:false},200,requestId(req),{"Cache-Control":"no-store"})}
