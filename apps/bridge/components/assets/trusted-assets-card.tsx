"use client";
import {useTrustedTokens} from "@/hooks/use-trusted-tokens";
import {CryptoAssetIcon} from "./crypto-asset-icon";
export function TrustedAssetsCard(){const state=useTrustedTokens();return <section className="pc-glass rounded-[26px] p-5"><div className="flex items-end justify-between gap-3"><div><p className="text-[10px] font-bold uppercase tracking-[.16em] text-slate-400">Allowlist</p><h2 className="mt-1 text-lg font-semibold text-slate-950 dark:text-white">Trusted token list</h2><p className="mt-1 text-xs text-slate-500 dark:text-slate-400">PowerChain assets use bundled artwork. SOL, SUI and USDC resolve through branded Web3 Icons; a licensed cryptoicons.cc URL template can be enabled as an optional image source.</p></div><span className="text-xs text-slate-400">{state.tokens.length} assets</span></div><div className="mt-4 grid gap-2 sm:grid-cols-2">{state.tokens.map(token=><div key={token.id} className="flex items-center gap-3 rounded-2xl border border-slate-200 bg-white/70 p-3 transition hover:bg-white dark:border-white/8 dark:bg-white/[.035] dark:hover:bg-white/[.055]"><span className="grid size-9 shrink-0 place-items-center"><CryptoAssetIcon token={token} size={36}/></span><div className="min-w-0"><p className="text-sm font-semibold text-slate-950 dark:text-white">{token.symbol} <span className="font-normal text-slate-400">· {token.chain}</span></p><p className="truncate font-mono text-[10px] text-slate-400">{token.address}</p></div></div>)}</div></section>}
