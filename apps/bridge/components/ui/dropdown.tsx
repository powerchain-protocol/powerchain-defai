"use client";
import { useEffect,useRef,useState,type ReactNode } from "react";
import { cn } from "./cn";
export function Dropdown({ trigger, children, align="end", label }: { trigger:ReactNode; children:ReactNode; align?:"start"|"end"; label:string }) { const [open,setOpen]=useState(false); const ref=useRef<HTMLDivElement>(null); useEffect(()=>{const close=(e:MouseEvent)=>{if(ref.current&&!ref.current.contains(e.target as Node))setOpen(false)}; const key=(e:KeyboardEvent)=>{if(e.key==="Escape")setOpen(false)}; document.addEventListener("mousedown",close);document.addEventListener("keydown",key);return()=>{document.removeEventListener("mousedown",close);document.removeEventListener("keydown",key)}},[]); return <div ref={ref} className="relative inline-block"><button type="button" aria-haspopup="menu" aria-expanded={open} aria-label={label} onClick={()=>setOpen(v=>!v)}>{trigger}</button>{open?<div role="menu" className={cn("absolute z-50 mt-2 min-w-48 rounded-[var(--pc-radius-card)] border border-slate-200 bg-white p-1.5 shadow-[0_20px_60px_rgba(7,16,13,.16)] dark:border-white/10 dark:bg-[#0a100d]",align==="end"?"right-0":"left-0")}>{children}</div>:null}</div>; }
export function DropdownItem({ children,onSelect,disabled=false }:{children:ReactNode;onSelect?:()=>void;disabled?:boolean}) { return <button type="button" role="menuitem" disabled={disabled} onClick={onSelect} className="flex min-h-10 w-full items-center rounded-[var(--pc-radius-control)] px-3 text-left text-sm text-slate-700 transition hover:bg-slate-100 disabled:opacity-40 dark:text-slate-200 dark:hover:bg-white/[.06]">{children}</button>; }
