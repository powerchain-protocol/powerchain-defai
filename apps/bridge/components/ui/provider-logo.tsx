"use client";import {useState} from "react";import {PROVIDER_BRANDS} from "@/common/provider-branding";
export function ProviderLogo({provider,size=24}:{provider:string;size?:number}){const[failed,setFailed]=useState(false);const brand=PROVIDER_BRANDS[provider.toLowerCase()];if(!brand||failed)return <span className="grid rounded-lg bg-slate-100 text-[9px] font-bold uppercase text-slate-500 dark:bg-white/8 dark:text-slate-300" style={{width:size,height:size}}>{provider.slice(0,2)}</span>;return <img src={brand.logo} alt={`${brand.name} logo`} width={size} height={size} className="rounded-lg object-contain" referrerPolicy="no-referrer" onError={()=>setFailed(true)}/>}
