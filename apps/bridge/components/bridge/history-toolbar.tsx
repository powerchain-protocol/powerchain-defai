"use client";
import { Input } from "@/components/ui/input";
import { Select } from "@/components/ui/select";
export type HistoryStatus = "ALL" | "IN_PROGRESS" | "COMPLETED" | "FAILED";
export function HistoryToolbar({query,status,onQueryChange,onStatusChange,onClear,onRefresh,refreshing=false,resultCount}:{query:string;status:HistoryStatus;onQueryChange:(value:string)=>void;onStatusChange:(value:HistoryStatus)=>void;onClear?:()=>void;onRefresh?:()=>void;refreshing?:boolean;resultCount?:number}) {
  const filtered=Boolean(query)||status!=="ALL";
  return <div><div className="flex flex-col gap-2 sm:flex-row sm:items-center" role="search" aria-label="Transfer history filters"><label className="relative min-w-0 flex-1"><span className="sr-only">Search transfers</span><Input value={query} onChange={(e) => onQueryChange(e.target.value.slice(0,160))} placeholder="Search transfer ID or wallet" className="h-11" /></label><label><span className="sr-only">Transfer status</span><Select value={status} onChange={(e) => onStatusChange(e.target.value as HistoryStatus)} className="h-11 min-w-40"><option value="ALL">All statuses</option><option value="IN_PROGRESS">In progress</option><option value="COMPLETED">Completed</option><option value="FAILED">Needs attention</option></Select></label>{filtered && onClear ? <button type="button" onClick={onClear} className="h-11 rounded-xl px-3 text-sm font-semibold text-slate-600 hover:bg-slate-100 dark:text-slate-300 dark:hover:bg-white/[.05]">Clear</button> : null}{onRefresh ? <button type="button" onClick={onRefresh} disabled={refreshing} className="pc-button-light h-11 rounded-xl px-4 text-sm font-semibold disabled:opacity-50">{refreshing ? "Refreshing…" : "Refresh"}</button> : null}</div>{typeof resultCount === "number" ? <p aria-live="polite" className="mt-2 text-xs text-slate-500 dark:text-slate-400">{resultCount} transfer{resultCount === 1 ? "" : "s"} shown</p> : null}</div>;
}
