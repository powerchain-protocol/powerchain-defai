"use client";
import {useConnectedWallets} from "@/lib/wallet/connected-wallets";
import {usePortfolio} from "@/hooks/use-portfolio";
import {formatTokenAmount} from "@/lib/tokens/trusted-token-list";
export function MultichainPortfolioCard(){const wallets=useConnectedWallets(),portfolio=usePortfolio(wallets.solanaAddress,wallets.suiAddress);return <section className="pc-glass overflow-hidden rounded-[26px]" aria-labelledby="portfolio-title"><div className="flex items-center justify-between gap-3 border-b border-slate-200/70 px-5 py-4 dark:border-white/8"><div><p className="text-[10px] font-bold uppercase tracking-[.16em] text-slate-400">Portfolio</p><h2 id="portfolio-title" className="mt-1 text-base font-semibold text-slate-950 dark:text-white">Trusted assets</h2></div><button type="button" onClick={()=>void portfolio.refresh()} disabled={portfolio.loading} className="pc-button-light rounded-xl px-3 py-2 text-xs font-semibold disabled:opacity-50">{portfolio.loading?"Refreshing…":"Refresh"}</button></div><div className="grid gap-px bg-slate-200/70 dark:bg-white/8 sm:grid-cols-2 lg:grid-cols-3">{portfolio.data?.balances.map(balance=><div key={balance.tokenId} className="bg-white/75 p-4 dark:bg-[#080d0b]/90"><div className="flex items-center justify-between gap-2"><span className="text-xs font-semibold text-slate-500 dark:text-slate-400">{balance.chain}</span><span className="rounded-full border border-slate-200 px-2 py-0.5 text-[10px] text-slate-500 dark:border-white/10 dark:text-slate-400">Trusted</span></div><p className="mt-3 text-xl font-semibold tracking-tight text-slate-950 dark:text-white">{formatTokenAmount(balance.balanceBaseUnits,balance.decimals)} <span className="text-sm text-slate-500">{balance.symbol}</span></p></div>)}{!wallets.solanaAddress&&!wallets.suiAddress?<div className="col-span-full bg-white/75 p-5 text-sm text-slate-500 dark:bg-[#080d0b]/90">Connect a Solana or Sui wallet to load the trusted-asset portfolio.</div>:null}</div>{portfolio.error?<p className="border-t border-rose-200 bg-rose-50 px-5 py-3 text-xs text-rose-700 dark:border-rose-500/20 dark:bg-rose-500/10 dark:text-rose-200">{portfolio.error}</p>:null}</section>}
