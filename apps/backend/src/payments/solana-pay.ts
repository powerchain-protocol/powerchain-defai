export type SolanaPayTransfer={recipient:string;amount?:string;splToken?:string;reference?:string;label?:string;message?:string;memo?:string};
export function buildSolanaPayUrl(input:SolanaPayTransfer){if(!input.recipient)throw new Error("SOLANA_PAY_RECIPIENT_REQUIRED");const url=new URL(`solana:${input.recipient}`);if(input.amount)url.searchParams.set("amount",input.amount);if(input.splToken)url.searchParams.set("spl-token",input.splToken);if(input.reference)url.searchParams.set("reference",input.reference);if(input.label)url.searchParams.set("label",input.label);if(input.message)url.searchParams.set("message",input.message);if(input.memo)url.searchParams.set("memo",input.memo);return url.toString()}
