# Program runtime isolation validation — 2026-08-17

- Aggregate program verification isolates failures per program.
- Added allowlisted `GET /api/v1/programs/readiness/:programId`.
- Runtime items expose deterministic checked-at and verification-duration evidence.
- Optional staking/escrow verifier failure cannot hide valid core Bridge evidence.
- Protocol UI supports independent program refresh and core/optional filtering.
- No source identifier is treated as runtime deployment evidence.
