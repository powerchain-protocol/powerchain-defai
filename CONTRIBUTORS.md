# Contributors

PowerChain Bridge is maintained by the PowerChain project contributors.

Contributions must preserve the fixed `1.0.0` first-party package version unless a release-version change is explicitly approved, and must preserve the bridge safety invariants documented in `README.md`.
